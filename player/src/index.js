const xpath = require('../node_modules/simple-xpath-position/lib/xpath.js');const fire = (event) => {    let target = xpath.fromNode(event.target);    console.log(event.type, target);};const attachHandlers = () => {    let eventTypes = ['click', 'keypress'];    let triggers = {};    eventTypes.map(type => triggers[type] = fire);    $('html').on(triggers);}$(() => {    attachHandlers();});